include_rules

: foreach *.c |> $(CC) $(FLAGS) -I./lib -I./external -c %f -o %o |> %B.o
: main.o app.o external/*.o lib/*.o |> $(CC) %f $(LIBS) -static -o %o |> typo-guard
: external/*.o lib/*.o tests/*.o |> $(CC) %f $(LIBS) -static -o %o |> run-tests
